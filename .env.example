# 复制此文件为 .env.local（已在 .gitignore 中）
#
# 这里不再有任何管理员凭证。VITE_* 变量会被打包进客户端 bundle，
# 谁都能从 dist 里读出来，所以密码只能放在服务端（见 server/.env.example）。
# 管理员在页面上输入账号密码 → 后端校验 → 签发 30 天 HMAC token。

# WebSocket 服务地址（必填）
# 游戏逻辑跑在服务端，前端必须连上后端才能运行。
#
# 本地开发：先在 server/ 下 `bun run dev`，然后：
# （/api 为同源相对路径，经 vite 代理转到 localhost:9000）
VITE_WS_URL=/api
#
# 生产：VITE_WS_URL=wss://你的域名/api
